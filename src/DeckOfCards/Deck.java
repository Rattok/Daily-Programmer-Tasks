package DeckOfCards;

public class Deck {

    // 52 Cards, Shuffle, truncate, Shuffle it, sort it (rank, color, suit), trump suit, take a card, discard, locations (stock, dealer, hand, discard), and counts on locations. Next up is coding some rulesets for different games.
}
